apiVersion: apps/v1
kind: Deployment
metadata:
  name: cart-service
  namespace: retail-decomposed
  labels:
    app: cart-service
    tier: backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: cart-service
  template:
    metadata:
      labels:
        app: cart-service
        tier: backend
    spec:
      serviceAccountName: retail-decomposed-sa
      containers:
      - name: cart
        image: acrretaildecomposed.azurecr.io/retail-decomposed-cart:latest
        ports:
        - containerPort: 8082
          name: http
        env:
        - name: ASPNETCORE_URLS
          value: "http://+:8082"
        - name: ProductsServiceUrl
          valueFrom:
            configMapKeyRef:
              name: retail-decomposed-config
              key: PRODUCTS_SERVICE_URL
        envFrom:
        - configMapRef:
            name: retail-decomposed-config
        - secretRef:
            name: retail-decomposed-secrets
        resources:
          requests:
            cpu: 100m
            memory: 256Mi
          limits:
            cpu: 500m
            memory: 512Mi
        livenessProbe:
          httpGet:
            path: /health
            port: 8082
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 8082
          initialDelaySeconds: 15
          periodSeconds: 5

---
apiVersion: v1
kind: Service
metadata:
  name: cart-service
  namespace: retail-decomposed
  labels:
    app: cart-service
spec:
  type: ClusterIP
  ports:
  - port: 8082
    targetPort: 8082
    protocol: TCP
    name: http
  selector:
    app: cart-service
