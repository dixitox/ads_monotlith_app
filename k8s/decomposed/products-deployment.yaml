apiVersion: apps/v1
kind: Deployment
metadata:
  name: products-service
  namespace: retail-decomposed
  labels:
    app: products-service
    tier: backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: products-service
  template:
    metadata:
      labels:
        app: products-service
        tier: backend
    spec:
      serviceAccountName: retail-decomposed-sa
      containers:
      - name: products
        image: acrretaildecomposed.azurecr.io/retail-decomposed-products:latest
        ports:
        - containerPort: 8081
          name: http
        env:
        - name: ASPNETCORE_URLS
          value: "http://+:8081"
        envFrom:
        - configMapRef:
            name: retail-decomposed-config
        - secretRef:
            name: retail-decomposed-secrets
        resources:
          requests:
            cpu: 100m
            memory: 256Mi
          limits:
            cpu: 500m
            memory: 512Mi
        livenessProbe:
          httpGet:
            path: /health
            port: 8081
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 8081
          initialDelaySeconds: 15
          periodSeconds: 5

---
apiVersion: v1
kind: Service
metadata:
  name: products-service
  namespace: retail-decomposed
  labels:
    app: products-service
spec:
  type: ClusterIP
  ports:
  - port: 8081
    targetPort: 8081
    protocol: TCP
    name: http
  selector:
    app: products-service
